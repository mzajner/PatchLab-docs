{
  "name": "patchlab-docs",
  "type": "module",
  "version": "0.0.1",
  "description": "The official PatchLab product guide, DSP learning path, and block reference.",
  "scripts": {
    "dev": "astro dev",
    "start": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "astro": "astro",
    "sync:registry": "node scripts/sync-registry.mjs",
    "sync:examples": "node scripts/sync-examples.mjs",
    "check:content": "node scripts/check-content.mjs",
    "check:accuracy": "node scripts/verify-accuracy.mjs && node scripts/test-dsp.mjs && node scripts/test-theme.mjs",
    "release:audit": "node scripts/release-audit.mjs",
    "release:audit:strict": "node scripts/release-audit.mjs --strict",
    "check": "npm run check:content && npm run check:accuracy && astro check && npm run build"
  },
  "dependencies": {
    "@astrojs/starlight": "^0.41.3",
    "astro": "^7.0.2",
    "sharp": "^0.34.5"
  },
  "devDependencies": {
    "@astrojs/check": "^0.9.9",
    "typescript": "^6.0.3"
  }
}
